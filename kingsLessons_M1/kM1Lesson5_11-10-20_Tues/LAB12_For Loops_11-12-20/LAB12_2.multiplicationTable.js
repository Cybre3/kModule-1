function main(num) {

    // let multiple = 0;
    

    // for (i = 1; i <= 10; i++) {
    //     multiple += 1;
    //     console.log(`${num} x ${multiple} = ${num * multiple}`);
    // }

        for (let i = 1; i <= 10; i++) {
            console.log(`${num} x ${i} = ${num * i}`);
        }
    
}

main(2);