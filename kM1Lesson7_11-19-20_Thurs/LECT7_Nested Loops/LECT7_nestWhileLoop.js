let rows = 1;
let cols = 2;

while (rows <= 2) {
    console.log('row: ' + rows);
    let cols = 1;
    while (cols <= 3) {
        console.log('   column: ' + cols);
        cols++;
    }
    rows++;
}